# App Name

To change the display name
* Open `AndroidManifest.xml` in the manifests folder
* Find `android:label` and either change file there, add a new String value resource for the app and change there